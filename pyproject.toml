[project]
name = "dash-v"
version = "0.1.0"
description = "Personal analytics dashboard built with Plotly Dash and SQLite."
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115,<1.0",
  "uvicorn>=0.32,<1.0",
]
